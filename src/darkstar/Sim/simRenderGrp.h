#pragma once

#include "simBase.h"

class SimRenderImage {
};

class SimRenderDefaultImage : public SimRenderImage {
};

struct SimRenderQueryImage : SimQuery {
};

class SimRenderSet : public SimSet {
};
